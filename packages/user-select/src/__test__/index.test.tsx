import React from 'react';
import { render } from '@testing-library/react';
import UserSelect from '../index';

describe('Button', () => {
  it('render content', () => {
    render(<UserSelect />);
  });
});
